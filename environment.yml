name: notebook
channels:
  - conda-forge
  - pytorch

dependencies:
  - llama-cpp-python
  - pip
  - pip:
      - nbgitpuller==1.3.0b1
      - transformers==4.46.3
      - openai>=1.0
      - anthropic>=0.8
      - python-dotenv
      - langchain>=0.1
      - sentence-transformers==3.4.1
      - otter-grader==6.1.6
      - huggingface_hub
      - gradio
      - docutils<0.20
      - onnxruntime
      - chromadb 
      - langchain-text-splitters 
      - langchain-community
      - datasets

